import type { Account } from '../auth/context';
export const testAccount: Account = { id: 'test-admin', username: 'test-admin', displayName: 'Test Administrator', role: 'admin', disabled: false, mustChangePassword: false };
